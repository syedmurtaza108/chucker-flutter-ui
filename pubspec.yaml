name: chucker_flutter_ui
description: The UI module for chucker_flutter packages available on pub. Using this module one can make his own Chucker UI for any package. 
version: 1.0.0
homepage: https://github.com/syedmurtaza108/chucker-flutter-ui

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^2.4.0